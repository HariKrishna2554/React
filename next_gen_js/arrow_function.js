export function addition(first_num, second_num) {
    console.log(first_num + second_num)
  }
  
  addition(2, 4)

export const sub = (first_num, second_num) => {
    console.log(first_num - second_num);
}

export const div = (first_num, second_num) => first_num / second_num;

export const multiply = (first_num, second_num) => first_num * second_num;
